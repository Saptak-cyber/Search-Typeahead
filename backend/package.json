{
  "name": "search-typeahead-backend",
  "version": "1.0.0",
  "description": "Search typeahead backend: Express + PostgreSQL + distributed Redis cache (consistent hashing)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "ingest": "node scripts/ingest.js",
    "gen-synthetic": "node scripts/gen-synthetic.js",
    "loadtest": "node scripts/loadtest.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "pg": "^8.12.0",
    "pg-copy-streams": "^6.0.6"
  },
  "devDependencies": {
    "autocannon": "^7.15.0"
  }
}
